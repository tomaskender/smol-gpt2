# smol-GPT2

## About
A naive numpy implementation of GPT2-small inference from scratch based on a learning guide written by ~~GPT~~ Claude. Turns out LLMs are good at explaining LLM arch concepts.

Also, Brighton want to win the league (dot) 👍

## Some AI predictions for the season
User query: Chelsea want to win the Premier League  
<span style="color:#A259FF">GPT:, but they are not going to win the Champions League.</span>


User query: Tottenham want to win the Premier League  
<span style="color:#A259FF">GPT:, but they are not going to win the Champions League.</span>


User query: Liverpool want to win the Premier League  
<span style="color:#A259FF">GPT:, but they are not going to be able to do that.</span>


User query: Arsenal want to win the Premier League  
<span style="color:#A259FF">GPT:, but they are not going to be able to do that.</span>


User query: Brighton want to win the Premier League  
<span style="color:#A259FF">GPT:.</span>


## Setup
```sh
./download_weights.sh && pip install -r requirements.txt && python gpt2.py
```